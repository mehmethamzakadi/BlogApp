# ============================================
# BlogApp - Base Docker Compose Configuration
# ============================================
# Bu dosya temel servis tanımlarını içerir.
# Kullanım:
#   Local:  docker-compose -f docker-compose.local.yml up -d
#   Prod:   docker-compose -f docker-compose.prod.yml up -d
# ============================================

x-common-healthcheck: &common-healthcheck
  interval: 10s
  timeout: 5s
  retries: 10
  start_period: 10s

x-restart-policy: &restart-policy
  restart: unless-stopped

services:
  # ===========================================
  # API Service
  # ===========================================
  blogapp.api:
    image: ${DOCKER_REGISTRY:-}blogappapi:${TAG:-latest}
    build:
      context: .
      dockerfile: src/BlogApp.API/Dockerfile
    <<: *restart-policy
    depends_on:
      postgresdb:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      redis.cache:
        condition: service_healthy

  # ===========================================
  # PostgreSQL Database
  # ===========================================
  postgresdb:
    image: postgres:16-alpine
    container_name: blogapp_postgres
    <<: *restart-policy
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-BlogAppDb}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      PGDATA: /var/lib/postgresql/data/pgdata
    command:
      - "postgres"
      - "-c"
      - "max_connections=300"
      - "-c"
      - "shared_buffers=256MB"
      - "-c"
      - "effective_cache_size=1GB"
      - "-c"
      - "work_mem=4MB"
      - "-c"
      - "maintenance_work_mem=128MB"
      - "-c"
      - "checkpoint_completion_target=0.9"
      - "-c"
      - "wal_buffers=16MB"
      - "-c"
      - "default_statistics_target=100"
      - "-c"
      - "random_page_cost=1.1"
      - "-c"
      - "effective_io_concurrency=200"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-BlogAppDb}"]
      <<: *common-healthcheck

  # ===========================================
  # RabbitMQ Message Broker
  # ===========================================
  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: blogapp_rabbitmq
    hostname: rabbitmq
    <<: *restart-policy
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_DEFAULT_USER:-blogapp}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_DEFAULT_PASS:-supersecret}
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "check_running"]
      <<: *common-healthcheck

  # ===========================================
  # Redis Cache
  # ===========================================
  redis.cache:
    image: redis:7-alpine
    container_name: blogapp_redis
    <<: *restart-policy
    command: >
      redis-server
      --appendonly yes
      --maxmemory 256mb
      --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      <<: *common-healthcheck

  # ===========================================
  # Seq Log Management
  # ===========================================
  seq:
    image: datalust/seq:latest
    container_name: blogapp_seq
    <<: *restart-policy
    environment:
      ACCEPT_EULA: "Y"
      SEQ_FIRSTRUN_ADMINPASSWORD: ${SEQ_ADMIN_PASSWORD:-Admin123!}
    volumes:
      - seq_data:/data

# ===========================================
# Volumes
# ===========================================
volumes:
  postgres_data:
    name: blogapp_postgres_data
  rabbitmq_data:
    name: blogapp_rabbitmq_data
  redis_data:
    name: blogapp_redis_data
  seq_data:
    name: blogapp_seq_data

# ===========================================
# Networks
# ===========================================
networks:
  default:
    name: blogapp_network
    driver: bridge
