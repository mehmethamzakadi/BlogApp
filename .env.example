# ============================================
# BlogApp Environment Variables Template
# ============================================
# Kopyalayın: cp .env.example .env
# Değerleri doldurun ve .env dosyasını Git'e eklemeyin!
#
# Local development için:
#   docker-compose -f docker-compose.local.yml up -d
#
# Production için:
#   docker-compose -f docker-compose.prod.yml up -d
# ============================================

# ===========================================
# Docker Registry (Production)
# ===========================================
DOCKER_REGISTRY=
TAG=latest

# ===========================================
# ASP.NET Core
# ===========================================
# Development | Staging | Production
ASPNETCORE_ENVIRONMENT=Production
ASPNETCORE_URLS=http://0.0.0.0:8080

# ===========================================
# PostgreSQL Database
# ===========================================
POSTGRES_DB=BlogAppDb
POSTGRES_USER=postgres
# Güçlü bir şifre kullanın! Örnek: openssl rand -base64 32
POSTGRES_PASSWORD=

# ===========================================
# Redis Cache
# ===========================================
# Güçlü bir şifre kullanın! Örnek: openssl rand -base64 32
REDIS_PASSWORD=

# ===========================================
# RabbitMQ Message Queue
# ===========================================
RABBITMQ_DEFAULT_USER=blogapp
# Güçlü bir şifre kullanın! Örnek: openssl rand -base64 32
RABBITMQ_DEFAULT_PASS=

# ===========================================
# Seq Logging
# ===========================================
# Seq admin paneli için şifre
SEQ_ADMIN_PASSWORD=

# ===========================================
# JWT Authentication
# ===========================================
# Minimum 32 karakter uzunluğunda olmalı!
# Örnek: openssl rand -base64 64
TOKEN_SECURITY_KEY=

# ===========================================
# SMTP Email (Opsiyonel)
# ===========================================
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@yourdomain.com

# ===========================================
# External Services (Opsiyonel)
# ===========================================
# Sentry DSN (Error Tracking)
SENTRY_DSN=

# Application Insights (Azure)
APPLICATIONINSIGHTS_CONNECTION_STRING=
